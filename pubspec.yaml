name: mosul_scanner
description: Mosul Scanner App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.7
  image: ^4.1.7

  pdf: ^3.10.8
  printing: ^5.11.1

  google_mlkit_document_scanner: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
