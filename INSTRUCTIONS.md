# تعليمات مشروع الماسح والطباعة - مكتب علاء الحديدي

## 1. قواعد معالجة الصور والقص التلقائي
- الاعتماد الأساسي في الكاميرا والمسح على مكتبة `google_mlkit_document_scanner`.
- ممنوع استخدام Enums معقدة مثل `DocumentFormat` أو `ScannerMode` في `DocumentScannerOptions` لتجنب أخطاء التوافق بين الإصدارات.
- يجب أن تعمل الكاميرا والمعرض (الاستوديو) عبر واجهة Google Scanner الذكية تلقائيًا.

## 2. قواعد الواجهة والطباعة
- الحفاظ على أبعاد ورقة A4 التفاعلية (210 × 297 مم).
- تطبيق تقويس الحواف (Corner Radius) على البطاقة الموحدة وبطاقة السكن في مساحة العمل وفي ملف الـ PDF المطبوع.
- توفير أزرار القياسات السريعة في الشريط الجانبي (بطاقة موحدة 8.5 × 5.4 سم، بطاقة سكن 8.8 × 5.8 سم).

## 3. معالجة الأخطاء والـ Fallback
- في حال عدم توفر خدمات Google Play على جهاز المستخدم، يجب إظهار رسالة تنبيهية (SnackBar) توضح السبب بدلاً من الانهيار الصامت.
- التأكد من مطابقة جميع ملفات الـ Dart (`main.dart`, `crop_engine.dart`, `hybrid_engine.dart`) وتناسق الأنواع (Types) والـ Imports قبل كل عملية Build.
